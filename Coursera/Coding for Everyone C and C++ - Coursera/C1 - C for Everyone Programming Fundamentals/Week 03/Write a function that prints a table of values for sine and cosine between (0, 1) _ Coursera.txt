#include<stdio.h>
#include<math.h> /* has  sin(), abs(), and fabs() */
int main(void)
{ 
double interval;
int i;
for(i = 1; i <30; i++)
{
 interval = i*10.0;
 printf("sin( %0.2lf ) = %0.2lf \tcos( %0.2lf ) = %0.2lf\n", interval,sin(interval),interval,cos(interval));
}

return 0;
}